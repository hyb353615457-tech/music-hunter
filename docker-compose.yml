services:
  music-hunter:
    build: .
    ports:
      - "3721:3721"
    environment:
      - NODE_ENV=production
      - PORT=3721
      - LOG_LEVEL=info
    restart: unless-stopped
    # 资源限制 - 适合低配服务器
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'
